# إزاي تعملي أول حساب أدمن + حسابات المديرات

## ليه مفيش صفحة "تسجيل حساب جديد"؟
عشان محدش يقدر يعمل نفسه أدمن أو مديرة براند من غير موافقتك. إنشاء الحسابات بيتم يدوياً من الأدمن (يعني إنتي دلوقتي) عن طريق Firebase Console.

## خطوات عمل حساب (أدمن أو مديرة):

### 1. اعملي المستخدم في Firebase Authentication
- روحي لـ Firebase Console → Authentication → Users → Add User
- حطي إيميل وباسورد
- هيديكي "User UID" (نسخيه)

### 2. اعملي Document ليه في Firestore
- روحي لـ Firestore Database → ابدئي Collection اسمها `users`
- Document ID = نفس الـ UID اللي نسختيه
- الحقول (Fields):

**لو أدمن:**
```
name: "اسمك"
phone: "01xxxxxxxxx"
role: "admin"
```

**لو مديرة براند:**
```
name: "اسم المديرة"
phone: "01xxxxxxxxx"
role: "brand_manager"
brandId: "id-البراند-بتاعها"   ← لازم تكوني عملتي البراند في collection اسمه brands الأول
```

## الترتيب الصح لما توافقي على طلب انضمام:
1. طلب الانضمام بيوصلك في `joinRequests` (هيظهر في لوحة الأدمن لما نبنيها)
2. توافقي → تعملي حساب ليها في Authentication (خطوة 1)
3. تعملي Document في `brands` بإسم البراند بتاعها
4. تعملي Document في `users` وتربطيها بالـ brandId بتاع البراند (خطوة 2)

هالحقّها كلها في لوحة الأدمن لاحقاً عشان تبقى زرار واحد بدل الخطوات اليدوية دي.
