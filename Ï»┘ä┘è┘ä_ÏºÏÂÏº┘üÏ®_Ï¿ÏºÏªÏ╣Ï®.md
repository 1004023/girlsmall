# دليل إضافة بائعة جديدة (خطوات ثابتة تتكرر كل مرة)

## 1. لما يوصلك طلب انضمام
- سجلي دخول كأدمن → لوحة الأدمن → شوفي الطلب
- لو موافقة: دوسي "موافقة" (ده بس بيغيّر حالة الطلب، مش بيعمل حساب فعلي)

## 2. اعمليها حساب دخول (Authentication)
Firebase Console → Authentication → Users → Add user
- إيميل + باسورد
- ✅ انسخي الـ **User UID** اللي هيطلعلك (هتحتاجيه في خطوة 4)

## 3. اعملي البراند بتاعها (Firestore)
Firestore Database → Data → داخل collection اسمها `brands` → Add document
- Document ID: Auto-ID
- الحقول:
  - `name` (string) → اسم البراند
  - `ownerName` (string) → اسمها هي
  - `category` (string) → واحدة من: `hijab-outing` / `home-wear` / `skincare` / `flowers` / `bags`
- Save
- ✅ انسخي الـ **Document ID** بتاع البراند ده (هتحتاجيه في خطوة 4)

## 4. اربطيها بحسابها (Firestore)
Firestore Database → Data → داخل collection اسمها `users` → Add document
- Document ID: الصقي فيه الـ **User UID** من خطوة 2 (نفسه بالظبط)
- الحقول:
  - `name` (string) → اسمها
  - `role` (string) → `brand_manager`
  - `brandId` (string) → الـ Document ID بتاع البراند من خطوة 3
- Save

## 5. ابعتيلها بياناتها
كلميها على واتساب وابعتيلها:
- الإيميل والباسورد اللي عملتيهم لها
- تقولها تدخل على لينك الموقع → تسجيل الدخول → هتوصل مباشرة للوحتها

---
**ملحوظة:** لو غيّرتي رأيك ومحدش هتوافقي عليها، خطوة 1 بس كفاية (دوسي "رفض" بدل "موافقة")، ومحتاجاش تكملي باقي الخطوات.
